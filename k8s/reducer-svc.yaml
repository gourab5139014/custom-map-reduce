apiVersion: v1
kind: Service
metadata:
  name: reducer-svc
  namespace: mapreduce
  labels:
    app: reducer
spec:
  type: NodePort
  selector:
    app: reducer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
